# Netlify deployment configuration
[build]
  command = "npm run build"
  publish = "dist"

# Use a modern Node version for the Vite build
[build.environment]
  NODE_VERSION = "20"

# SPA fallback: serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
